const Discord = require('discord.js');

module.exports = {
    name: "test",
    aliases: ["t"],
    cooldown: 3,
    devOnly: true,
    async execute(message, args, data, client) {
        
    },
};